<UserControl x:Class="JolimontBikeRace.App.Views.RaceManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.2*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Race Manager" Style="{StaticResource SectionHeaderStyle}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.4*" />
            </Grid.ColumnDefinitions>

            <!-- Left column: race list with filter and Add / Duplicate / Delete buttons. -->
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Text="{Binding RaceFilterText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Filter races..." Margin="0,0,0,10" />

                    <ListBox Grid.Row="1" ItemsSource="{Binding Races}" SelectedItem="{Binding SelectedRace}"
                             DisplayMemberPath="Name" BorderThickness="0" />

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Add" Command="{Binding AddRaceCommand}" Margin="0,0,8,0" />
                        <Button Content="Duplicate" Command="{Binding DuplicateRaceCommand}" Margin="0,0,8,0" />
                        <Button Content="Delete" Style="{StaticResource DangerButtonStyle}" Command="{Binding DeleteRaceCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right column: selected race name editor and linked-categories checkbox list. -->
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBox Width="320" Text="{Binding SelectedRaceName, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Command="{Binding SaveRaceNameCommand}" Margin="8,0,0,0" />
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="Categories offered in this race" FontWeight="SemiBold" Margin="0,0,0,8" />

                    <ItemsControl Grid.Row="2" ItemsSource="{Binding LinkedCategories}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding CategoryName}" IsChecked="{Binding IsLinked}" Margin="0,4" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="Categories" Style="{StaticResource SectionHeaderStyle}" Margin="0,24,0,16" />

        <!-- Bottom: categories master grid with Add / Delete buttons. -->
        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                        <DataGridTextColumn Header="Minimum Bib Number" Binding="{Binding MinimumBibNumber}" Width="1*" />
                        <DataGridTextColumn Header="Maximum Bib Number" Binding="{Binding MaximumBibNumber}" Width="1*" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Add" Command="{Binding AddCategoryCommand}" Margin="0,0,8,0" />
                    <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Command="{Binding SaveCategoryCommand}" Margin="0,0,8,0" />
                    <Button Content="Delete" Style="{StaticResource DangerButtonStyle}" Command="{Binding DeleteCategoryCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
